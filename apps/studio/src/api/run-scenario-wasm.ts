// Re-exported from the shared package so Studio and CLI use the same runner.
export { runScenarioWasm } from "@bpmnkit/engine/wasm-runner"
export type { ScenarioLike, ScenarioResultLike } from "@bpmnkit/engine/wasm-runner"
